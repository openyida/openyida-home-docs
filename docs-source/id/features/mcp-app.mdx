---
title: "MCP App Server"
description: "Dokumen ini menjelaskan MCP App Server di OpenYida, termasuk alur penggunaan, perintah, dan hal yang perlu diperhatikan."
icon: puzzle-piece
---

# MCP App Server

Halaman ini menjelaskan MCP App Server di OpenYida, termasuk tujuan, alur penggunaan, dan hal penting saat membangun aplikasi Yida dengan AI.

## Ikhtisar

Dokumen ini menjelaskan MCP App Server di OpenYida, termasuk alur penggunaan, perintah, dan hal yang perlu diperhatikan.

## Poin penting

- Gunakan halaman ini untuk memahami konteks utama sebelum menerapkannya di proyek nyata.
- Nama perintah, parameter, kode, dan beberapa nama produk tetap menggunakan bahasa Inggris agar mudah dicari dan disalin dengan benar.
- Saat bekerja dengan宜搭, periksa izin, organisasi, dan lingkungan sebelum publikasi.

## Penggunaan

Ikuti langkah dalam dokumen ini bersama OpenYida CLI dan konfigurasi organisasi. Jika ada contoh kode di bawah, contoh tersebut dapat disalin lalu disesuaikan dengan kebutuhan.

## Perintah dan contoh

Contoh berikut mempertahankan perintah dan kode asli untuk menjaga akurasi teknis.

```json
{
  "mcpServers": {
    "openyida": {
      "command": "npx",
      "args": ["-y", "@openyida/mcp-app", "--stdio"]
    }
  }
}
```

```bash
npx @openyida/mcp-app
# Server listening on http://localhost:3001/mcp
```

```bash
PORT=8080 npx @openyida/mcp-app
```

```json
[
  { "type": "TextField", "label": "Name", "required": true },
  { "type": "NumberField", "label": "Age", "required": false }
]
```

```json
{
  "type": "text",
  "text": "Error: <error message>"
}
```
