---
title: "Panduan integrasi DingTalk CLI"
description: "Dokumen ini menjelaskan Panduan integrasi DingTalk CLI di OpenYida, termasuk alur penggunaan, perintah, dan hal yang perlu diperhatikan."
icon: book-open
---

# Panduan integrasi DingTalk CLI

Halaman ini menjelaskan Panduan integrasi DingTalk CLI di OpenYida, termasuk tujuan, alur penggunaan, dan hal penting saat membangun aplikasi Yida dengan AI.

## Ikhtisar

Dokumen ini menjelaskan Panduan integrasi DingTalk CLI di OpenYida, termasuk alur penggunaan, perintah, dan hal yang perlu diperhatikan.

## Poin penting

- Gunakan halaman ini untuk memahami konteks utama sebelum menerapkannya di proyek nyata.
- Nama perintah, parameter, kode, dan beberapa nama produk tetap menggunakan bahasa Inggris agar mudah dicari dan disalin dengan benar.
- Saat bekerja dengan宜搭, periksa izin, organisasi, dan lingkungan sebelum publikasi.

## Penggunaan

Ikuti langkah dalam dokumen ini bersama OpenYida CLI dan konfigurasi organisasi. Jika ada contoh kode di bawah, contoh tersebut dapat disalin lalu disesuaikan dengan kebutuhan.

## Perintah dan contoh

Contoh berikut mempertahankan perintah dan kode asli untuk menjaga akurasi teknis.

```bash
openyida dws install
```

```bash
    curl -fsSL https://raw.githubusercontent.com/DingTalk-Real-AI/dingtalk-workspace-cli/main/scripts/install.sh | sh
    ```

```bash
    irm https://raw.githubusercontent.com/DingTalk-Real-AI/dingtalk-workspace-cli/main/scripts/install.ps1 | iex
    ```

```bash
    export DWS_CLIENT_ID="<your-app-key>"
    export DWS_CLIENT_SECRET="<your-app-secret>"
    ```

```bash
    openyida dws auth login --client-id <your-app-key> --client-secret <your-app-secret>
    ```

```bash
    openyida dws auth login
    ```

```bash
# Search contacts
openyida dws contact user search --keyword "张三"

# List departments
openyida dws contact department list

# Get user details
openyida dws contact user get --userid <userId>
```

```bash
# List calendar events
openyida dws calendar event list

# Create an event
openyida dws calendar event create --title "团队会议" --start-time "2026-03-29T10:00:00Z"

# Query free/busy status
openyida dws calendar free-busy query --userids <userId1>,<userId2>
```
