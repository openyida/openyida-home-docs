---
title: "Menggunakan OpenCode"
description: "Dokumen ini menerangkan Menggunakan OpenCode dalam OpenYida, termasuk aliran penggunaan, arahan dan perkara yang perlu diberi perhatian."
icon: opencode
---

# Menggunakan OpenCode

Halaman ini menerangkan Menggunakan OpenCode dalam OpenYida, termasuk tujuan, aliran penggunaan dan perkara penting semasa membina aplikasi Yida dengan AI.

## Gambaran keseluruhan

Dokumen ini menerangkan Menggunakan OpenCode dalam OpenYida, termasuk aliran penggunaan, arahan dan perkara yang perlu diberi perhatian.

## Perkara penting

- Gunakan halaman ini untuk memahami konteks utama sebelum menggunakannya dalam projek sebenar.
- Nama arahan, parameter, kod dan beberapa nama produk dikekalkan dalam bahasa Inggeris supaya mudah dicari dan disalin dengan tepat.
- Apabila bekerja dengan宜搭, semak kebenaran, organisasi dan persekitaran sebelum penerbitan.

## Penggunaan

Ikuti langkah dalam dokumen ini bersama OpenYida CLI dan konfigurasi organisasi. Jika terdapat contoh kod di bawah, contoh tersebut boleh disalin dan disesuaikan mengikut keperluan.

## Arahan dan contoh

Contoh berikut mengekalkan arahan dan kod asal untuk memastikan ketepatan teknikal.

```bash
    # Homebrew (macOS)
    brew install opencode

    # Or Go install
    go install github.com/opencode-ai/opencode@latest
    ```

```bash
    export ANTHROPIC_API_KEY="your API key"
    ```

```bash
    # OpenAI
    export OPENAI_API_KEY="your key"

    # DeepSeek
    export OPENAI_API_KEY="your key"
    export OPENAI_BASE_URL="https://api.deepseek.com/v1"
    ```

```bash
    npm install -g openyida
    ```

```
    Help me log in to Yida
    ```

```
Create a procurement system on Yida:
- Purchase request: applicant, item name, quantity, budget, purpose
- Approval workflow: Department Manager → Finance approval
```

```
Create an event registration app with a signup form and registration statistics dashboard
```

```
Show me all my apps on Yida
```
