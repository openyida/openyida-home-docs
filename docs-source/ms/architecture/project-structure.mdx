---
title: "Struktur projek"
description: "Dokumen ini menerangkan Struktur projek dalam OpenYida, termasuk aliran penggunaan, arahan dan perkara yang perlu diberi perhatian."
icon: folder-tree
---

# Struktur projek

Halaman ini menerangkan Struktur projek dalam OpenYida, termasuk tujuan, aliran penggunaan dan perkara penting semasa membina aplikasi Yida dengan AI.

## Gambaran keseluruhan

Dokumen ini menerangkan Struktur projek dalam OpenYida, termasuk aliran penggunaan, arahan dan perkara yang perlu diberi perhatian.

## Perkara penting

- Gunakan halaman ini untuk memahami konteks utama sebelum menggunakannya dalam projek sebenar.
- Nama arahan, parameter, kod dan beberapa nama produk dikekalkan dalam bahasa Inggeris supaya mudah dicari dan disalin dengan tepat.
- Apabila bekerja dengan宜搭, semak kebenaran, organisasi dan persekitaran sebelum penerbitan.

## Penggunaan

Ikuti langkah dalam dokumen ini bersama OpenYida CLI dan konfigurasi organisasi. Jika terdapat contoh kod di bawah, contoh tersebut boleh disalin dan disesuaikan mengikut keperluan.

## Arahan dan contoh

Contoh berikut mengekalkan arahan dan kod asal untuk memastikan ketepatan teknikal.

```
openyida/
├── src/
│   ├── commands/          # CLI command implementations
│   │   ├── create-app.ts
│   │   ├── create-form.ts
│   │   ├── create-page.ts
│   │   ├── publish.ts
│   │   ├── share.ts
│   │   ├── data.ts
│   │   ├── connector.ts
│   │   ├── report.ts
│   │   ├── doctor.ts
│   │   └── login.ts
│   ├── core/              # Core functionality modules
│   │   ├── yida-api.ts    # Yida API wrapper
│   │   ├── ai-service.ts  # AI service integration
│   │   ├── config.ts      # Configuration management
│   │   └── auth.ts        # Authentication handling
│   ├── utils/             # Utility functions
│   │   ├── logger.ts
│   │   ├── file.ts
│   │   └── validator.ts
│   └── types/             # TypeScript type definitions
│       ├── yida.ts
│       ├── ai.ts
│       └── index.ts
├── skills/                # Skill system
│   ├── builtin/           # Built-in Skills
│   └── custom/            # Custom Skills
├── templates/             # Code templates
│   ├── app/
│   ├── form/
│   └── page/
├── docs/                  # Documentation assets
├── tests/                 # Test files
├── package.json
├── tsconfig.json
└── README.md
```

```
skills/
├── builtin/               # Built-in Skills
│   ├── code-generator/    # Code generation
│   ├── form-builder/      # Form building
│   ├── page-designer/     # Page design
│   └── data-analyzer/     # Data analysis
└── custom/                # User custom Skills
    └── (user extensions)
```

```json
{
  "defaultOrg": "org-default",
  "orgs": {
    "org-default": {
      "accessToken": "xxx",
      "refreshToken": "xxx",
      "expiresAt": 1234567890
    }
  },
  "aiProvider": "openai",
  "aiConfig": {
    "apiKey": "sk-xxx",
    "model": "gpt-4"
  }
}
```

```json
{
  "appId": "APP-XXX",
  "defaultEnv": "dev",
  "environments": {
    "dev": { "appKey": "xxx", "appSecret": "xxx" },
    "prod": { "appKey": "xxx", "appSecret": "xxx" }
  }
}
```
