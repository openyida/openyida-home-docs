---
title: "الموجهات الشائعة"
description: "ملخص موجهات اللغة الطبيعية المدعومة من OpenYida، تشمل إنشاء التطبيقات وعمليات النماذج وإدارة البيانات والموصلات والتقارير."
icon: "comments"
---

صُمم OpenYida للتفاعل باللغة الطبيعية. تم التحقق من صحة الموجهات التالية ويمكن استخدامها في جميع أدوات الذكاء الاصطناعي المدعومة.

---

## إنشاء التطبيقات

```
ساعدني في بناء تطبيق xxx
```
```
ساعدني في بناء CRM
```
```
ساعدني في بناء تطبيق حاسبة رواتب شخصية
```
```
ساعدني في إنشاء نظام IPD على Yida لإدارة العملية الكاملة لإنتاج الرقائق
```
```
إنشاء تطبيق بناءً على مستند المتطلبات: [لصق PRD]
```
```
إنشاء لعبة تهنئة عيد ميلاد على Yida
```
```
بناء لعبة تخمين ألغاز عيد الفوانيس
```

---

## عمليات النماذج والصفحات

### إنشاء صفحات النماذج

```
ساعدني في إنشاء صفحة نموذج xxx
```
```
إنشاء نموذج طلب إجازة يتضمن: اسم الموظف، القسم، تاريخ البدء، تاريخ الانتهاء، سبب الإجازة
```

### إضافة حقول النموذج

```
أضف حقل [اسم الحقل] إلى [اسم الصفحة]، نوع الحقل: [TextField/NumberField/DateField/SelectField/...]
```
```
أضف حقل "القسم" من نوع القائمة المنسدلة إلى نموذج معلومات الموظف، الخيارات: قسم الهندسة، قسم التسويق، قسم المبيعات، قسم الموارد البشرية
```

<Note>
يتم قبول [أنواع الحقول المدعومة](/ar/features/skills#أنواع-الحقول-المدعومة) فقط. الأنواع غير المدعومة (مثل `MobileField` و `EmailField`) سيتم رفضها.
</Note>

### تعديل خصائص الحقل

```
اجعل حقل [اسم الحقل] في [اسم الصفحة] إلزاميًا
```
```
اجعل حقل [اسم الحقل] في [اسم الصفحة] للقراءة فقط
```
```
غيّر تسمية حقل [اسم الحقل] في [اسم الصفحة] إلى [تسمية جديدة]
```

### صفحات العرض المخصصة

```
أنشئ صفحة عرض مخصصة لـ [اسم الصفحة]
```
```
إنشاء صفحة هبوط لمنتج التعاون المؤسسي
```

---

## النشر والمشاركة

```
نشر صفحة [اسم الصفحة]
```
```
اجعل صفحة [اسم الصفحة] قابلة للوصول العام
```
```
تكوين أذونات الوصول العام لصفحة [اسم الصفحة] — يمكن لأي شخص الوصول عبر الرابط
```

---

## تحويل闪记 إلى PRD

```
تحويل ملاحظات DingTalk闪记 إلى مستندات PRD
```

```
إنشاء PRD من نص الاجتماع: [لصق الملاحظات]
```

```
إنشاء مستند متطلبات المشروع من ملاحظة闪记 المرفقة
```

```
تحويل ملاحظات الاجتماع إلى مستند متطلبات منتج منظم
```

<Note>
يتطلب تسجيل الدخول. يقرأ الأمر نصوص الاجتماعات ويستخدم الذكاء الاصطناعي لإنشاء مستندات PRD منظمة. يتم حفظ الإخراج في دليل `prd/`.
</Note>

---

## إدارة البيانات

```
استعلام عن جميع السجلات في [اسم النموذج]
```
```
تصدير بيانات [اسم النموذج] إلى Excel
```
```
حذف جميع بيانات الاختبار في [اسم النموذج]
```

---

## التكامل والأتمتة

### إنشاء تدفق التكامل

```
إنشاء تدفق تكامل يرسل إشعارات عند إرسال سجلات جديدة
```

```
إعداد الأتمتة على [اسم النموذج] لإخطار [الفريق/المستخدم] عند التقديمات الجديدة
```

```
إنشاء سير عمل [اسم النموذج] يؤدي إلى إرسال إشعارات بريد إلكتروني إلى [المستلمين]
```

### إضافة إجراءات آلية

```
إضافة إجراء البحث عن البيانات إلى تكامل [اسم التدفق]
```

```
إضافة إجراء إنشاء السجل لإنشاء إدخالات تلقائيًا في [النموذج المستهدف]
```

<Note>
تدفقات التكامل يتم تشغيلها عند أحداث النماذج (إدراج، تحديث، حذف) ويمكنها تنفيذ الإشعارات والبحث عن البيانات وإنشاء السجلات. استخدم CLI لإنشاء وتكوين التدفقات.
</Note>

---

## الموصلات (تكامل HTTP)

```
إنشاء موصل HTTP لـ [اسم API]، عنوان URL الأساسي: [url]
```
```
أضف إجراء POST إلى [اسم الموصل] لإنشاء السجلات
```
```
إنشاء موصل بذكاء من هذا أمر curl: [لصق curl]
```

---

## التقارير والمخططات

```
إنشاء تقرير Yida لـ [اسم التطبيق] يتضمن مخططًا شريطيًا لعمليات الإرسال الشهرية
```
```
إضافة مخطط دائري يعرض توزيع الحالات إلى تقرير [اسم التقرير]
```

---

## الحساب والبيئة

```
تسجيل الدخول مرة أخرى / تسجيل الخروج
```
```
التبديل إلى منظمة [اسم المنظمة]
```
```
عرض حالة تسجيل الدخول الحالية
```
```
تشغيل تشخيص البيئة
```

---

## تكامل DingTalk

### البحث والاستعلام

```
البحث عن جهة اتصال باسم [الاسم]
```

```
سرد جميع أحداث التقويم
```

```
الاستعلام عن طلبات الموافقة الخاصة بي
```

### الإنشاء والإدارة

```
إنشاء مهمة مهام: [وصف المهمة]
```

```
إرسال رسالة: [محتوى الرسالة]
```

### استعلام الحضور

```
سرد سجلات الحضور
```

```
التحقق من حالة الحضور الخاصة بي
```

<Info>
يتطلب أمر `dws` تثبيت DingTalk CLI بشكل منفصل. قم بتشغيل `openyida dws install` إذا لم يكن مثبتًا بالفعل. الإخراج افتراضيًا بتنسيق جدول؛ أضف `-f json` للحصول على إخراج JSON.
</Info>

---

## CDN والموارد

```
تكوين رفع صور CDN لـ [اسم التطبيق]
```
```
رفع [ملف الصورة] إلى CDN
```

---

<Tip>
**نصيحة:** كلما زاد السياق المقدم، كانت النتائج أفضل. بدلاً من قول "إنشاء نموذج"، قل "إنشاء نموذج موافقة إجازة يتضمن اسم الموظف والقسم ونوع الإجازة (قائمة منسدلة: إجازة سنوية/إجازة مرضية/إجازة شخصية) وتواريخ البدء والانتهاء والسبب — اجعل اسم الموظف والتاريخ إلزاميين".
</Tip>