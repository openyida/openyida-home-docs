---
title: "Cursor kullanımı"
description: "Bu belge OpenYida içinde Cursor kullanımı konusunu; kullanım akışı, komutlar ve dikkat edilmesi gereken noktalarla açıklar."
icon: cursor
---

# Cursor kullanımı

Bu sayfa OpenYida içinde Cursor kullanımı konusunu açıklar; amaç, kullanım akışı ve AI ile Yida uygulaması geliştirirken dikkat edilmesi gereken noktaları kapsar.

## Genel bakış

Bu belge OpenYida içinde Cursor kullanımı konusunu; kullanım akışı, komutlar ve dikkat edilmesi gereken noktalarla açıklar.

## Önemli noktalar

- Gerçek bir projeye uygulamadan önce ana bağlamı anlamak için bu sayfayı kullanın.
- Komut adları, parametreler, kod ve bazı ürün adları doğru arama ve kopyalama için İngilizce bırakılmıştır.
- 宜搭 ile çalışırken yayımlamadan önce izinleri, organizasyonu ve ortamı kontrol edin.

## Kullanım

Bu dokümandaki adımları OpenYida CLI ve organizasyon ayarlarıyla birlikte izleyin. Aşağıdaki kod örnekleri doğrudan kopyalanıp ihtiyaca göre uyarlanabilir.

## Komutlar ve örnekler

Aşağıdaki örnekler teknik doğruluğu korumak için özgün komut ve kodu saklar.

```bash
    npm install -g openyida
    ```

```
    Help me log in to Yida
    ```

```
Create a project management system on Yida:
- Project form: project name, owner, start date, end date, status
- Task form: linked to project, task name, priority, assignee, due date
- Create a project overview dashboard page
```

```
Help me create a meeting room booking app
```

```
Add a refund approval workflow to the "Order Management" app
```

```json
{
  "mcpServers": {
    "openyida": {
      "command": "npx",
      "args": ["-y", "@openyida/mcp-app", "--stdio"]
    }
  }
}
```
