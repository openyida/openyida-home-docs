---
title: "Частые вопросы"
description: "Этот документ описывает Частые вопросы в OpenYida: сценарии использования, команды и важные замечания."
icon: circle-question
---

# Частые вопросы

Эта страница описывает Частые вопросы в OpenYida: назначение, рабочий процесс и важные замечания при создании приложений Yida с помощью AI.

## Обзор

Этот документ описывает Частые вопросы в OpenYida: сценарии использования, команды и важные замечания.

## Ключевые моменты

- Используйте эту страницу, чтобы понять основной контекст перед применением в реальном проекте.
- Имена команд, параметры, код и часть названий продуктов оставлены на английском, чтобы их можно было точно найти и скопировать.
- При работе с宜搭 перед публикацией проверьте права, организацию и выбранное окружение.

## Использование

Следуйте шагам из документа вместе с OpenYida CLI и настройками организации. Если ниже есть примеры кода, их можно скопировать и адаптировать под задачу.

## Команды и примеры

Следующие примеры сохраняют исходные команды и код для технической точности.

```bash
    openyida org list       # List all organizations and corpIds
    openyida org switch --corp-id <corpId>   # Switch to target organization
    openyida env            # Confirm the switch
    ```

```
    Add a [field name] field to [page name], field type: [TextField/NumberField/DateField/SelectField/...], make it required
    ```

```
    Make [page name] page publicly accessible
    ```

```bash
    openyida verify-short-url <appType> <formUuid> /o/my-public-page
    openyida save-share-config <appType> <formUuid> /o/my-public-page y n
    ```

```bash
    openyida verify-short-url <appType> <formUuid> /s/my-dashboard
    openyida save-share-config <appType> <formUuid> /s/my-dashboard y
    openyida get-page-config <appType> <formUuid>
    ```

```bash
    openyida update-form-config <appType> <formUuid> false "Page Title"
    ```

```bash
    openyida update-form-config <appType> <formUuid> true "Page Title"
    ```
