---
title: "Быстрый старт DingTalk CLI"
description: "Этот документ описывает Быстрый старт DingTalk CLI в OpenYida: сценарии использования, команды и важные замечания."
icon: play
---

# Быстрый старт DingTalk CLI

Эта страница описывает Быстрый старт DingTalk CLI в OpenYida: назначение, рабочий процесс и важные замечания при создании приложений Yida с помощью AI.

## Обзор

Этот документ описывает Быстрый старт DingTalk CLI в OpenYida: сценарии использования, команды и важные замечания.

## Ключевые моменты

- Используйте эту страницу, чтобы понять основной контекст перед применением в реальном проекте.
- Имена команд, параметры, код и часть названий продуктов оставлены на английском, чтобы их можно было точно найти и скопировать.
- При работе с宜搭 перед публикацией проверьте права, организацию и выбранное окружение.

## Использование

Следуйте шагам из документа вместе с OpenYida CLI и настройками организации. Если ниже есть примеры кода, их можно скопировать и адаптировать под задачу.

## Команды и примеры

Следующие примеры сохраняют исходные команды и код для технической точности.

```bash
    openyida dws install
    ```

```bash
    curl -fsSL https://raw.githubusercontent.com/DingTalk-Real-AI/dingtalk-workspace-cli/main/scripts/install.sh | sh
    ```

```bash
        export DWS_CLIENT_ID="your-client-id"
        export DWS_CLIENT_SECRET="your-client-secret"
        ```

```bash
        openyida dws auth login --client-id your-client-id --client-secret your-client-secret
        ```

```bash
    openyida dws auth login
    ```

```bash
# Basic search
openyida dws contact user search --keyword "张三"

# JSON output (suitable for AI Agents)
openyida dws contact user search --keyword "张三" -f json

# Save to file
openyida dws contact user search --keyword "张三" -o contacts.json
```

```bash
# Create a personal to-do
openyida dws todo task create --title "准备周报" --executors "your-userid"

# Create a team to-do
openyida dws todo task create --title "项目评审" --executors "userid1,userid2,userid3"

# List all to-dos
openyida dws todo task list
```

```bash
# Query today's check-in records
openyida dws attendance record list

# Query a specific user's attendance
openyida dws attendance record list --userids "your-userid"
```
