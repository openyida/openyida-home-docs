---
title: "Использование OpenCode"
description: "Этот документ описывает Использование OpenCode в OpenYida: сценарии использования, команды и важные замечания."
icon: opencode
---

# Использование OpenCode

Эта страница описывает Использование OpenCode в OpenYida: назначение, рабочий процесс и важные замечания при создании приложений Yida с помощью AI.

## Обзор

Этот документ описывает Использование OpenCode в OpenYida: сценарии использования, команды и важные замечания.

## Ключевые моменты

- Используйте эту страницу, чтобы понять основной контекст перед применением в реальном проекте.
- Имена команд, параметры, код и часть названий продуктов оставлены на английском, чтобы их можно было точно найти и скопировать.
- При работе с宜搭 перед публикацией проверьте права, организацию и выбранное окружение.

## Использование

Следуйте шагам из документа вместе с OpenYida CLI и настройками организации. Если ниже есть примеры кода, их можно скопировать и адаптировать под задачу.

## Команды и примеры

Следующие примеры сохраняют исходные команды и код для технической точности.

```bash
    # Homebrew (macOS)
    brew install opencode

    # Or Go install
    go install github.com/opencode-ai/opencode@latest
    ```

```bash
    export ANTHROPIC_API_KEY="your API key"
    ```

```bash
    # OpenAI
    export OPENAI_API_KEY="your key"

    # DeepSeek
    export OPENAI_API_KEY="your key"
    export OPENAI_BASE_URL="https://api.deepseek.com/v1"
    ```

```bash
    npm install -g openyida
    ```

```
    Help me log in to Yida
    ```

```
Create a procurement system on Yida:
- Purchase request: applicant, item name, quantity, budget, purpose
- Approval workflow: Department Manager → Finance approval
```

```
Create an event registration app with a signup form and registration statistics dashboard
```

```
Show me all my apps on Yida
```
