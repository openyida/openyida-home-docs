---
title: "การใช้ Codex"
description: "เอกสารนี้อธิบาย การใช้ Codex ใน OpenYida พร้อมแนวทางการใช้งาน คำสั่ง และข้อควรทราบที่เกี่ยวข้อง"
icon: codex
---

# การใช้ Codex

หน้านี้อธิบาย การใช้ Codex ของ OpenYida โดยสรุปเป้าหมาย ขั้นตอนการใช้งาน และข้อควรระวังที่เกี่ยวข้องสำหรับการสร้างแอป Yida ด้วย AI

## ภาพรวม

เอกสารนี้อธิบาย การใช้ Codex ใน OpenYida พร้อมแนวทางการใช้งาน คำสั่ง และข้อควรทราบที่เกี่ยวข้อง

## ประเด็นสำคัญ

- ใช้หน้านี้เพื่อทำความเข้าใจบริบทหลักก่อนเริ่มลงมือในโครงการจริง
- ชื่อคำสั่ง พารามิเตอร์ โค้ด และชื่อผลิตภัณฑ์บางส่วนคงไว้เป็นภาษาอังกฤษเพื่อให้ค้นหาและคัดลอกได้ถูกต้อง
- เมื่อใช้งานกับ宜搭จริง ให้ตรวจสอบสิทธิ์ องค์กร และสภาพแวดล้อมก่อนเผยแพร่

## การใช้งาน

ปฏิบัติตามขั้นตอนในเอกสารนี้ร่วมกับคำสั่ง OpenYida CLI และการตั้งค่าขององค์กร หากมีตัวอย่างโค้ดด้านล่าง สามารถคัดลอกไปปรับใช้ได้โดยตรง

## คำสั่งและตัวอย่าง

ตัวอย่างต่อไปนี้คงคำสั่งและโค้ดต้นฉบับไว้เพื่อความถูกต้องทางเทคนิค

```bash
    npm install -g openyida
    ```

```
    Help me log in to Yida
    ```

```
@Yida Help me create a project management system
```

```
Build an employee training management system on Yida, including a course schedule, registration form, and assessment scores table
```

```
Help me create a CRM system:
- Lead management: name, company, phone, source, follow-up status
- Customer profiles: linked to leads, industry, annual revenue, account manager
- Opportunity tracking: linked to customer, amount, stage, expected close date
```

```
Add a data analytics dashboard to my app showing this month's total opportunity amount and close rate
```

```
Help me configure an approval workflow: leave requests over 3 days require department manager approval
```

```bash
    ls ~/.openyida/codex-plugin/plugins/openyida/.codex-plugin/plugin.json
    ```
