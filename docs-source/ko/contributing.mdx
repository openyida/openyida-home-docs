---
title: "기여 가이드"
description: "OpenYiDA에 기여하는 방법을 알아봅니다. 버그 보고, 기능 제안, 문서 개선, 스킬 확장, 개발 환경 설정 가이드를 포함합니다."
icon: "handshake"
---

OpenYiDA에 오신 것을 환영합니다! 기여에 관심을 가져주셔서 감사합니다. 이 가이드는 시작하는 데 필요한 모든 것을 다룹니다.

## 빠른 링크

- **GitHub:** https://github.com/openyida/openyida
- **Issues:** https://github.com/openyida/openyida/issues
- **npm:** https://www.npmjs.com/package/openyida

## 기여 방법

1. **버그 보고** → 재현 단계와 환경 정보를 포함하여 Issue 열기
2. **기능 제안** → 먼저 Discussion 또는 Issue 를 시작한 후 구현
3. **문서 개선** → 문서 관련 PR 은 항상 환영
4. **스킬 추가** → `yida-skills/` 아래에서 확장
5. **버그 수정 / 새 기능** → 아래 개발 워크플로우 따르기

---

## 개발 환경 설정

```bash
# 1. 저장소 Fork 및 클론
git clone git@github.com:your-username/openyida.git
cd openyida

# 2. 의존성 설치
npm install

# 3. 로컬 디버깅 위한 전역 링크
npm link

# 4. 테스트npm test
```

---

## PR 체크리스트

풀 리퀘스트를 제출하기 전에 확인:

- [ ] 실제 Yida 계정으로 로컬에서 관련 기능 테스트 완료
- [ ] 모든 테스트 통과: `npm test`
- [ ] JS 구문 검사 통과:
  ```bash
  node --check bin/yida.js && for f in lib/*.js lib/locales/*.js; do node --check "$f"; done
  ```
- [ ] PR 설명에 변경 사항과 이유가 명확히 설명됨
- [ ] UI/동작 변경 사항이 있는 경우샷 녹화 첨부

---

## PR 가이드라인

- **하나의 PR, 하나의 일** — 관련 없는 변경 사항을 섞지 않음
- **PR 제목 형식:** `feat: add xxx` / `fix: fix xxx` / `docs: update xxx`
- **설명** 에 포함: 무엇, 왜, 테스트 방법
- PR 이 Issue 를 닫는 경우, 설명에 `Closes #123` 추가

---

## 커밋

[Conventional Commits](https://www.conventionalcommits.org/) 따르기:

```
feat: add export-app command
fix: fix get-page-config path error
docs: update CLI command reference
refactor: refactor login module
test: add utils unit tests
chore: upgrade dependencies
```

---

## 코드

- 기존 코드 따르기 (CommonJS 모듈, Node.js 네이티브 API 선호)
- 변수와 함수에 의미 있는 영어 이름 사용; 약어 피하기
- 오류를 완전히 처리 — 예외를 조용히 삼키지 않음
- 새 명령 추가 시 `README.md` 의 CLI 명령 테이블 업데이트

---

## AI / Vibe-Coded PR 환영!

Claude Code, Cursor, Aone Copilot 또는 기타 AI 도우미 PR 을 완전히 환영합니다! PR 설명에 사용한 AI 도구를 언급해 주세요 — 유지 관리자가 생성 컨텍스트를 이해하는 데 도움이 됩니다.

---

## 라이선스

기여함으로써, 귀하의 기여를 [MIT License](https://github.com/openyida/openyida/blob/main/LICENSE) 에 따라 라이선스하는 데 동의합니다.

## 빠른 링크

- **GitHub:** https://github.com/openyida/openyida
- **Issues:** https://github.com/openyida/openyida/issues
- **npm:** https://www.npmjs.com/package/openyida

## 기여 방법

1. **버그 보고** → 재현 단계와 환경 정보를 포함하여 Issue 열기
2. **기능 제안** → 먼저 Discussion 또는 Issue를 시작한 다음 구현
3. **문서 개선** → 문서 PR는 항상 환영
4. **스킬 추가** → `yida-skills/` 아래의 스킬 팩 확장
5. **버그 수정/새 기능** → 아래 개발 워크플로우 따르기

---

## 개발 설정

```bash
# 1. 리포지토리 포크 및 복제
git clone git@github.com:your-username/openyida.git
cd openyida

# 2. 의존성 설치
npm install

# 3. 로컬 디버깅을 위해 전역으로 링크
npm link

# 4. 테스트 실행
npm test
```

---

## PR 체크리스트

풀 리퀘스트를 제출하기 전에 확인하세요：

- [ ] 실제 Yida 계정으로 로컬에서 관련 기능을 테스트함
- [ ] 모든 테스트 통과：`npm test`
- [ ] JS 구문 검사 통과：
  ```bash
  node --check bin/yida.js && for f in lib/*.js lib/locales/*.js; do node --check "$f"; done
  ```
- [ ] PR 설명에서 변경된 내용과 이유를 명확히 설명함
- [ ] UI/동작 변경이 있는 경우 스크린샷 또는 녹화 첨부

---

## PR 가이드라인

- **하나의 PR, 하나의 작업** — 관련 없는 변경을 혼합하지 마세요
- **PR 제목 형식：** `feat: add xxx` / `fix: fix xxx` / `docs: update xxx`
- **설명**은 무엇, 왜, 테스트 방법을 다루어야 함
- PR이 Issue를 해결하는 경우 설명에 `Closes #123` 추가

---

## 커밋 규약

[Conventional Commits](https://www.conventionalcommits.org/)를 따르세요：

```
feat: add export-app command
fix: fix get-page-config path error
docs: update CLI command reference
refactor: refactor login module
test: add utils unit tests
chore: upgrade dependencies
```

---

## 코드 스타일

- 기존 코드 스타일 따르기（CommonJS 모듈，Node.js 네이티브 API 우선）
- 변수와 함수에는 의미 있는 영어 이름 사용，약어 피하기
- 오류를 완전히 처리 — 예외를 조용히 삼키지 않기
- 새 명령을 추가할 때 `README.md`의 CLI 명령 테이블 업데이트

---

## AI / Vibe-Coded PR 환영！🤖

Claude Code, Cursor, Aone Copilot 또는 기타 AI 도구로 지원된 PR은 완전히 환영됩니다！PR 설명에서 사용한 AI 도구를 언급하세요 — 유지 관리자가 생성 컨텍스트를 이해하는 데 도움이 됩니다.

---

## 라이선스

기여함으로써 귀하는 [MIT 라이선스](https://github.com/openyida/openyida/blob/main/LICENSE) 하에 기여를 라이선스하는 데 동의합니다.
