---
title: "DingTalk CLI 통합 가이드"
description: "OpenYida에서 DingTalk CLI(dws)를 설치, 설정, 사용하는 방법에 대한 가이드"
icon: "book-open"
---

## 소개

OpenYida는 DingTalk 공식 CLI 도구(dws)를 통합했습니다. `openyida dws` 명령을 통해 DingTalk 워크스페이스의 모든 기능에 직접 접근할 수 있습니다.

## DingTalk CLI 설치

### 방법 1: openyida를 통한 설치 (권장)

```bash
openyida dws install
```

### 방법 2: 수동 설치

**macOS / Linux:**
```bash
curl -fsSL https://raw.githubusercontent.com/DingTalk-Real-AI/dingtalk-workspace-cli/main/scripts/install.sh | sh
```

**Windows (PowerShell):**
```bash
irm https://raw.githubusercontent.com/DingTalk-Real-AI/dingtalk-workspace-cli/main/scripts/install.ps1 | iex
```

## 빠른 시작

### 1. DingTalk 앱 자격 증명 설정

```bash
export DWS_CLIENT_ID="<your-app-key>"
export DWS_CLIENT_SECRET="<your-app-secret>"
```

또는 명령에서 직접 지정할 수 있습니다:
```bash
openyida dws auth login --client-id <your-app-key> --client-secret <your-app-secret>
```

### 2. 로그인 인증

```bash
openyida dws auth login
```

### 3. DingTalk API 사용

#### 주소록 관리
```bash
# 연락처 검색
openyida dws contact user search --keyword "张三"

# 부서 목록 조회
openyida dws contact department list

# 사용자 상세 정보 조회
openyida dws contact user get --userid <userId>
```

#### 캘린더 관리
```bash
# 캘린더 이벤트 목록 조회
openyida dws calendar event list

# 이벤트 생성
openyita dws calendar event create --title "团队会议" --start-time "2026-03-29T10:00:00Z"

# 여유/바쁨 상태 조회
openyida dws calendar free-busy query --userids <userId1>,<userId2>
```

#### 할 일 관리
```bash
# 할 일 생성
openyida dws todo task create --title "准备季度汇报材料" --executors "<userId>"

# 할 일 목록 조회
openyida dws todo task list

# 할 일 상태 업데이트
openyida dws todo task update --taskid <taskId> --status completed
```

#### 결재 워크플로우
```bash
# 결재 인스턴스 목록 조회
openyida dws approval instance list

# 결재 인스턴스 생성
openyida dws approval instance create --processCode <processCode> --formData <json>

# 결재 상세 정보 조회
openyida dws approval instance get --instanceId <instanceId>
```

#### 근태 관리
```bash
# 근태 기록 조회
openyida dws attendance record list

# 근무 일정 조회
openyida dws attendance shift list

# 근태 통계 조회
openyida dws attendance statistics list
```

#### 그룹 채팅 및 봇
```bash
# 봇 메시지 전송
openyida dws chat robot send --content "大家好！"

# 그룹 멤버 목록 조회
openyida dws chat member list --chatid <chatId>
```

## 출력 형식

모든 명령은 다양한 출력 형식을 지원합니다:

```bash
# 테이블 출력 (기본값, 사람이 읽기 좋은 형식)
openyida dws contact user search --keyword "张三" -f table

# JSON 출력 (AI Agent에 적합)
openyida dws contact user search --keyword "张三" -f json

# 원시 API 응답
openyida dws contact user search --keyword "张三" -f raw
```

## 고급 사용법

### 미리보기 (실행하지 않음)
```bash
openyida dws todo task list --dry-run
```

### 파일로 출력
```bash
openyida dws contact user search --keyword "李明" -o result.json
```

### 도움말 보기
```bash
# dws 전체 도움말 보기
openyida dws help

# 특정 서비스 도움말 보기
openyida dws contact --help

# 특정 명령 도움말 보기
openyida dws contact user search --help
```

## 지원 서비스

| 서비스 | 명령 접두사 | 기능 |
|--------|-----------|------|
| 주소록 | `contact` | 사용자/부서 관리 |
| 그룹 채팅 | `chat` | 그룹 관리/봇 메시지 |
| 캘린더 | `calendar` | 일정/회의실/여유-바쁨 조회 |
| 할 일 | `todo` | 작업 관리 |
| 결재 | `approval` | 결재 워크플로우 |
| 근태 | `attendance` | 출퇴근/근무 일정/통계 |
| DING | `ding` | DING 메시지 |
| 보고서 | `report` | 보고서 관리 |
| 워크벤치 | `workbench` | 애플리케이션 검색 |
| 개발자 문서 | `devdoc` | 문서 검색 |

추가 서비스는 `openyida dws --help`를 실행하여 확인하세요.

## 환경 변수

자주 사용되는 환경 변수:

| 변수 | 설명 |
|------|------|
| `DWS_CLIENT_ID` | DingTalk 앱 Client ID (AppKey) |
| `DWS_CLIENT_SECRET` | DingTalk 앱 Client Secret (AppSecret) |
| `DWS_CONFIG_DIR` | 사용자 정의 설정 디렉터리 |
| `DWS_TRUSTED_DOMAINS` | Bearer 토큰이 허용되는 도메인 목록 |

## 주의 사항

<Warning>
**기업 관리자 승인 필요**: 이 도구는 DingTalk 기업 데이터에 접근하므로 기업 관리자의 승인이 필요합니다.
</Warning>

<Warning>
**자격 증명 보안**: 자격 증명을 버전 관리 시스템에 커밋하지 마세요. 환경 변수 사용을 권장합니다.
</Warning>

<Tip>
AI Agent 시나리오에서는 `-f json` 형식 출력을 사용하는 것을 권장합니다.
</Tip>

<Info>
모든 오류는 구조화된 JSON으로 반환되어 프로그램에서 쉽게 처리할 수 있습니다.
</Info>

## 참고 자료

- [DingTalk CLI GitHub](https://github.com/DingTalk-Real-AI/dingtalk-workspace-cli)
- [DingTalk 오픈 플랫폼](https://open.dingtalk.com/)

## 자주 묻는 질문

### Q: dws 명령을 찾을 수 없다고 표시되면?
A: `openyida dws install`을 실행하여 DingTalk CLI를 설치하거나 수동으로 설치 스크립트를 실행하세요.

### Q: Client ID와 Secret은 어디서 얻나요?
A: DingTalk 오픈 플랫폼에서 기업 내부 앱을 생성한 후 앱 상세 페이지에서 확인할 수 있습니다.

### Q: 계정을 전환하려면?
A: `openyida dws auth logout`을 실행하여 현재 계정에서 로그아웃한 후 다시 로그인하세요.
