---
title: "Bắt Đầu Nhanh"
description: "Cài đặt OpenYida và xây dựng ứng dụng Yida đầu tiên của bạn trong 2 phút."
icon: "bolt"
---

## Điều kiện tiên quyết

Trước khi bắt đầu, hãy đảm bảo bạn đã cài đặt các phụ thuộc sau:

| Phụ thuộc | Phiên bản | Lệnh kiểm tra |
|------|------|----------|
| [Node.js](https://nodejs.org) | ≥ 18 | `node --version` |

<Tip>
  Đó là tất cả những gì bạn cần. OpenYida là triển khai thuần Node.js — không cần Python, không cần Playwright, không cần bước build.
</Tip>

---

## Bước 1 — Cài đặt

```bash
npm install -g openyida
```

Xác minh cài đặt:

```bash
openyida --version
```

---

## Bước 2 — Đăng nhập vào Yida

```bash
openyida login
```

OpenYida sẽ mở cửa sổ trình duyệt (hoặc hiển thị mã QR) để bạn xác thực bằng tài khoản Yida/Alibaba. Thông tin đăng nhập được lưu trữ cục bộ — bạn chỉ cần làm điều này một lần.

<Info>
  OpenYida detects QwenWork automatically. If QR login is needed, use `openyida login --qr` or `openyida login --agent-qr`.
</Info>

---

## Bước 3 — Kiểm tra môi trường

```bash
openyida env
```

Điều này phát hiện công cụ mã hóa AI hiện đang hoạt động (Claude Code, Cursor, QwenWork, v.v.) và hiển thị trạng thái đăng nhập hiện tại của bạn. Chạy lệnh này bất cứ khi nào bạn chuyển đổi môi trường.

---

## Bước 4 — Khởi tạo không gian làm việc dự án

```bash
openyida copy
```

Điều này sao chép mẫu không gian làm việc `project/` vào thư mục làm việc của công cụ AI hiện tại của bạn, cho phép AI truy cập cấu hình ứng dụng và mẫu trang của bạn.

---

## Bước 5 — Bắt đầu xây dựng

Bây giờ chỉ cần nói chuyện với trợ lý mã hóa AI của bạn. Dưới đây là một số prompt bắt đầu:

```
Xây dựng cho tôi một hệ thống IPD trên Yida để quản lý quy trình sản xuất chip hoàn chỉnh
```

```
Giúp tôi xây dựng một hệ thống CRM với leads, contacts và deals
```

```
Tạo một ứng dụng máy tính lương cá nhân
```

```
Tạo một mini-game chúc mừng sinh nhật trên Yida
```

AI sẽ sử dụng gói kỹ năng OpenYida để gọi các lệnh CLI đúng theo thứ tự: `create-app` → `create-form` / `create-page` → `publish` → `save-share-config`.

---

## Bước 6 — Xuất bản ứng dụng

Sau khi AI tạo ra ứng dụng, sử dụng lệnh sau để xuất bản:

```bash
openyida check-page project/pages/src/dashboard.oyd.jsx
openyida compile project/pages/src/dashboard.oyd.jsx
openyida publish project/pages/src/dashboard.oyd.jsx <appType> <formUuid>
```

Hoặc hỏi AI:
```
Xuất bản trang [tên trang]
Làm cho trang có thể truy cập công khai
```

---

## Chạy chẩn đoán

Nếu có gì đó không ổn, chạy công cụ chẩn đoán tích hợp:

```bash
openyida doctor
```

Nó kiểm tra phiên bản Node.js, trạng thái đăng nhập và cấu hình môi trường của bạn, và có thể tự động sửa các vấn đề phổ biến với `--fix`.

---

## Tích hợp OpenClaw

Nếu bạn đang sử dụng OpenClaw, cài đặt trực tiếp gói kỹ năng OpenYida:

```bash
npx clawhub@latest install nicky1108/yida-app
```

Sau đó sử dụng nó qua [yida-app trên ClawhHub](https://clawhub.ai/nicky1108/yida-app).

---

## Bước tiếp theo

<CardGroup cols={2}>
  <Card title="Prompt phổ biến" icon="comments" href="/vi/features/common-prompts">
    Duyệt qua danh sách đầy đủ các prompt ngôn ngữ tự nhiên được hỗ trợ bởi OpenYida.
  </Card>
  <Card title="Tham khảo CLI" icon="terminal" href="/vi/features/skills">
    Khám phá tất cả các lệnh CLI và mô-đun kỹ năng.
  </Card>
  <Card title="Trường hợp demo" icon="images" href="/vi/introduction/demo">
    Xem các ứng dụng thực tế được xây dựng với OpenYida.
  </Card>
  <Card title="Thực hành tốt nhất" icon="star" href="/vi/guides/best-practices">
    Mẹo để tận dụng tối đa việc tạo ứng dụng được điều khiển bởi AI.
  </Card>
</CardGroup>